# AI 产品作品集

静态网站文件，可直接部署到 GitHub Pages。

## GitHub Pages

1. 新建一个 GitHub 仓库。
2. 将本目录中的全部文件上传到仓库根目录。
3. 打开仓库 `Settings` -> `Pages`。
4. 在 `Build and deployment` 中选择 `Deploy from a branch`。
5. 选择 `main` 分支和根目录 `/ (root)`，然后保存。

部署完成后，GitHub 会显示网站访问地址。
