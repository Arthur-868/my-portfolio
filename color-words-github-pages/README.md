# Color Words

Open `index.html` directly in a browser, or run a local static server:

```powershell
python -m http.server 8000
```

Click the center word, press Space, or press the right arrow key to move to the next color scene.
